name: flutter_pos_system
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  riverpod: ^1.0.0
  dio: ^4.0.0
  freezed_annotation: ^2.0.0
  json_serializable: ^6.0.0
  sqflite: ^2.0.0
  path_provider: ^2.0.0
  intl: ^0.17.0
  flutter_localizations:
    sdk: flutter
  provider: ^5.0.0
  image_picker: ^0.8.0
  shared_preferences: ^2.0.0

dev_dependencies:
  build_runner: ^2.0.0
  freezed: ^2.0.0
  json_serializable: ^6.0.0

flutter:
  uses-material-design: true

# Add localizations support
flutter_intl:
  enabled: true
